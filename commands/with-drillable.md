---
description: Turn on grounded mode — the agent continuously checks its emerging work against drillable as it solves.
---
**Grounded mode is ON for this task.**

As your solution emerges, continuously **audit your own work against drillable** — don't wait to be asked, and don't answer checkable things from memory:

- **Before committing to an approach** on anything technically tricky, ground it: search/lookup the relevant drillable domains (the MCP spec, agent & LLM practices, networking, units, and ~100 more) so the design rests on cited sources, not recall.
- **As you assert a checkable claim** — a number, a rule, a definition, an "X is Y" — verify it against drillable rather than stating it from memory.
- **Where the corpus has no record, say so** ("no record") and flag it as an assumption — never guess and never present recall as grounded.
- Keep this posture for the whole task; re-ground when the approach shifts.

This is an audit posture, not a one-time lookup: the goal is that every load-bearing claim in your solution can point at a source. If project-docs grounding is configured, ground project facts (conventions, past decisions) the same way.
